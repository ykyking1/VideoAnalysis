# İHA Video Arşivinde Semantik Arama — GPU (CUDA) kurulumu
#
#     pip install -r requirements.txt
#     python -m scripts.check_env        # <- ATLAMAYIN
#
# Bu dosya CUDA'lı PyTorch kurar. Aşağıdaki --extra-index-url satırı sayesinde
# pip, torch'u PyPI yerine PyTorch'un CUDA index'inden çeker (+cu126 yerel sürüm
# etiketi PEP 440'ta düz sürümden BÜYÜK sayıldığı için CUDA derlemesi tercih
# edilir). Doğrulanmış: bu index'te en güncel torch 2.13.0+cu126.
#
# NEDEN BU KADAR VURGU: Düz `pip install torch` Windows'ta CPU derlemesi kurar
# ve HİÇBİR HATA VERMEZ - sistem çalışır, ingest ~15-20 kat yavaş koşar. Bir
# arşiv ölçeğinde bu aylar demektir. `scripts/check_env.py` bunu yakalar.
#
# Farklı CUDA sürümü gerekiyorsa (ör. Blackwell/RTX 50xx için cu128) aşağıdaki
# URL'i değiştirin: https://download.pytorch.org/whl/cu128
#
# CPU-only geliştirme/CI için: requirements-cpu.txt
# vLLM sunucusu (sorgu ayrıştırma/caption/rerank, Linux): requirements-serving.txt

--extra-index-url https://download.pytorch.org/whl/cu126

# --- Model çalıştırma (GPU) ---
torch>=2.9.0
torchvision>=0.24.0
transformers>=4.57          # Qwen3-VL bu sürümden itibaren destekli
accelerate>=0.34
huggingface-hub>=0.25
qwen-vl-utils>=0.0.14       # < 0.0.14 SESSİZCE bozuk embedding üretir
ultralytics>=8.3            # YOLO26
opencv-python>=4.10
pillow>=10.4

# --- Çekirdek ---
numpy>=1.26,<3
requests>=2.31
python-dotenv>=1.0

# --- Nesne deposu / vektör deposu / durum ---
minio>=7.2
qdrant-client>=1.12
psycopg[binary]>=3.1

# --- Orkestrasyon ---
temporalio>=1.7
confluent-kafka>=2.5

# --- Telemetri (proje-ozeti.md §3.1 madde 2) ---
pymavlink>=2.4
polars>=1.0
pysolar>=0.11
shapely>=2.0

# --- Değerlendirme (proje-ozeti.md §7) ---
ranx>=0.3

# --- Arayüz (scripts/query_ui.py, proje-ozeti.md §3.2) ---
# Saf Python - ek JS/frontend derlemesi gerekmiyor. GPU gerektirmiyor.
gradio>=5.0

# --- Geliştirme ---
pytest>=8.0
