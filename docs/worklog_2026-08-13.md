# Worklog — 2026-08-13

Konu: **AU-AIR aday veri seti — Kaggle T4'te 8 videonun tamamı, gerçek pipeline
ile.** `poc/kaggle_auair.ipynb` üzerinden çalıştırıldı (bkz. o defterin
docstring'i - lisans/varsayım notları AYNEN geçerli). Amaç: "AU-AIR bizim
kullanım amacımıza uyar mı" sorusunu mekanizma seviyesinde cevaplamak,
retrieval kalitesi iddiası DEĞİL.

## Sonuç: mekanizma çalışıyor, 8/8 video hatasız ingest edildi

| video | pencere | süre(s) | gerçek-zaman | araç (YOLO) |
|---|---|---|---|---|
| auair_frame_20190829091111 | 12 | 711.2 | 11.64x | 13 |
| auair_frame_20190905091750 | 22 | 1275.8 | 9.85x | 109 |
| auair_frame_20190905103112 | 25 | 1481.2 | 9.43x | 140 |
| auair_frame_20190905111947 | 3 | 156.4 | 6.63x | 10 |
| auair_frame_20190905112522 | 21 | 1201.6 | 8.88x | 133 |
| auair_frame_20190905142119 | 12 | 666.8 | 8.76x | 83 |
| auair_frame_20190905143505 | 11 | 625.2 | 9.38x | 102 |
| auair_frame_20190906150731 | 26 | 1547.2 | 9.37x | 229 |

Toplam: 132 pencere, 7665.4s video (≈2,13 saat), 814.5s işleme süresi.
**Agrege gerçek-zaman katsayısı: 9.411x.**

Çapraz doğrulama: 7665.4s (≈2,13 saat) daha önce annotations.json'dan
bağımsız olarak hesapladığımız "8 video, 2,13 saat" gerçek AU-AIR toplam
süresiyle birebir örtüşüyor - reconstruction ve pencereleme doğru
çalışıyor.

## §8'in 40x varsayımıyla karşılaştırma

- Yerel (zayıf GPU, T4 değil): **0.10x** (tek video, önceki oturum).
- Kaggle T4 (bu ölçüm, 8 video agrege): **9.411x**.
- proje-ozeti.md §8 varsayımı: **40x**.

Kaggle T4, yerel makineden ~94x daha hızlı - ama §8'in varsaydığından hâlâ
**~4x yavaş**. Bu artık gerçek bir GPU'da (T4) ölçülmüş bir sayı, yerel
ölçümden çok daha güvenilir - ama **hâlâ tek deneme**:

- `EMBEDDING_BATCH_SIZE=8` kalibre edilmedi (kaggle_pipeline_trial.ipynb'nin
  6. bölümündeki kalibrasyon adımı bu defterde YOK - AU-AIR defteri onu
  atlıyor). Batch büyütülürse katsayı artabilir.
- Video-başı katsayı 6.63x-11.64x arası dağılıyor (kısa videolarda proxy
  üretimi gibi sabit maliyetler oranca daha fazla yer kaplıyor) - N=8 küçük
  bir örneklem, varyansın gerçek dağılımı bilinmiyor.
- §8'in 40x'i hangi donanım/batch/model sürümü için verildiği belgelenmemiş
  (proje-ozeti.md §8 hâlâ "doğrulanmadı" işaretli) - iki sayı doğrudan
  kıyaslanabilir mi, net değil.

**Sonuç: §8'in 40x varsayımı hâlâ doğrulanmadı, ama şimdi gerçek bir üst
ölçüm noktamız var (T4/batch=8: 9.4x) - iyimser tarafta değil.**

## Sorgu testleri - niteliksel gözlem, kalite ölçümü DEĞİL

5 sorgu, `clips_auair_test` üzerinde (vLLM yok, tamamen semantik):

- "a truck at a roundabout" → en yüksek skor (0.771). AU-AIR zaten bir
  kavşak/roundabout trafik veri seti - tutarlı.
- "an ambulance on the road" → daha düşük skor (0.564). AU-AIR'in 8
  kategorisinde ("Human, Car, Truck, Van, Motorbike, Bicycle, Bus,
  Trailer") ambulans yok - modelin olmayan bir kavrama daha az güvenle
  eşleşmesi iyi işaret, ama N=1 sorgudan genelleme yapılmamalı.
- Diğer 3 sorgu ("cars waiting at an intersection", "a busy road with many
  vehicles", "a motorbike on the street") 0.54-0.68 arası skorlarla makul
  aralıkta, video_id'ler hep `auair_frame_...` formatında (koleksiyon
  izolasyonu doğru çalışıyor, gerçek/SeaDroneSee verisiyle karışma yok).

İlk sorgunun `embed=5847ms` olması modelin ilk kez yüklenmesinden
(`ingest_one()` sonunda `unload_model()` çağrılıyor) - sonraki sorgular
70-80ms, gerçek steady-state sorgu gecikmesi bu ikincisi.

**Bu bir retrieval kalite testi DEĞİL** - golden set yok (§7 hâlâ boş),
"doğru" cevap tanımlı değil. Sadece "modelin çıktısı tamamen rastgele/saçma
değil" gözlemi.

## Telemetri sanity kontrolü — 8/8 video referans aralığında

`clips_auair_test`'teki tüm 132 pencerenin payload'ı taranıp `agl_m`/
`avg_speed_kmh` video-başına min-max'ı `poc/auair_adapter.py`'nin
docstring'indeki referansla (~4.8-30.2m, ~0.05-16 km/h) karşılaştırıldı:

| video | agl_m | avg_speed_kmh |
|---|---|---|
| auair_frame_20190829091111 | 18.7-28.2 | 0.30-3.21 |
| auair_frame_20190905091750 | 7.2-12.2 | 0.28-1.10 |
| auair_frame_20190905103112 | 19.6-20.3 | 0.29-1.37 |
| auair_frame_20190905111947 | 24.9-29.0 | 0.53-1.73 |
| auair_frame_20190905112522 | 11.0-29.8 | 0.32-2.02 |
| auair_frame_20190905142119 | 11.4-29.7 | 0.93-5.34 |
| auair_frame_20190905143505 | 6.5-25.8 | 0.78-4.51 |
| auair_frame_20190906150731 | 12.2-29.7 | 0.30-2.69 |

Tümü: agl_m 6.5-29.8m, avg_speed_kmh 0.28-5.34 km/h — **referans aralığın
içinde, anomali yok.** Bu, `agl_m = altitude/1000` (mm varsayımı) ve
`speed_kmh = |linear_x,y,z| * 3.6` (m/s varsayımı) birim çıkarımlarını
DOĞRULAMIYOR (hâlâ AU-AIR'in resmi dokümantasyonunda açık yazmıyor) ama
**tutarsızlığı da göstermiyor** - 8 videonun tamamı fiziksel olarak makul
ve birbiriyle tutarlı bir aralıkta. Gerçek ölçülen hız aralığı (max 5.34
km/h) referansın üst sınırından (16 km/h) belirgin şekilde düşük - bu
görev profiliyle (düşük irtifa trafik gözetimi, çoğunlukla hovering/yavaş
seyir) tutarlı, ekstra bir makullük işareti.

## vLLM yapısal ayrıştırma — Kaggle'da ilk kez gerçekten çalıştı

Kurulum, `kaggle_pipeline_trial.ipynb`'nin 10. bölümüyle aynı (torch==2.11.0
sabitlemesi, Qwen2.5-7B-Instruct-AWQ, xgrammar) - önce `unload_model()` ile
embedding modeli boşaltılıp T4 16GB'de vLLM'e yer açıldı. **Bu, projenin bu
hattının ilk gerçek uçtan uca çalışması** - önceki oturumlarda Colab'da
`libcudart.so.13` ABI hatasıyla hiç ayağa kalkmamıştı.

4 sorgu, `clips_auair_test` üzerinde:

| sorgu | çıkan filtre | parse gecikmesi |
|---|---|---|
| "en az 10 arac gorunen kayitlar" | `min_vehicle_count=10` ✅ | 2812ms (soğuk*) |
| "20 metreden alcakta ucan" | `min_agl_m=0, max_agl_m=20` ✅ | 1170ms |
| "1000 arac olan goruntuler" | `min_vehicle_count=1000` (imkansiz) | 893ms |
| "a truck at a roundabout" | `min_vehicle_count=1` ⚠️ | 2495ms |

\* İlk sorgunun `embed=20870ms`'i embedding modelinin `unload_model()`
sonrası yeniden yüklenmesinden - gerçek steady-state embed maliyeti değil
(sonraki sorgularda 78-94ms).

**Doğru çalışanlar:**
- Sayısal eşik çıkarımı (`min_vehicle_count=10`, `max_agl_m=20`) tam
  beklenen alanlara doğru yazıldı.
- **Kademeli gevşetme vLLM çıktısıyla da doğru tetiklendi**: "1000 arac"
  (imkansiz) `min_vehicle_count=1000` filtresini üretti, 2 adımlık gevşetme
  sonrası `[yaklaşık]` işaretli 17 sonuç döndü - hard-filtre+relax
  mekanizması vLLM'in ürettiği filtrelerle de sorunsuz bütünleşiyor.

**Beklenmeyen bulgu:** "a truck at a roundabout" (tamamen semantik olması
beklenen, İngilizce genel notebook'taki eşdeğeri "a boat on the water" için
"filtre yok" çıkmıştı) burada `min_vehicle_count=1` üretti - model "truck"
kelimesinden bir araç-varlığı filtresi çıkarmış. Pratikte etkisiz (skor
sıralaması ve ilk sonuç, vLLM'siz aynı sorguyla BİREBİR aynı çıktı - top-1
yine `auair_frame_20190905111947 0:00-0:01:00 skor=0.771`) çünkü AU-AIR'de
neredeyse her pencerede en az 1 araç var. Ama bu, vLLM'in "tamamen semantik"
sorguları her zaman filtre-siz bırakmadığını, isim/nesne geçen her sorguya
gizli bir eşik ekleyebildiğini gösteriyor - §5'in "yapısal ayrıştırma
doğruluğu hiç ölçülmedi" notuna somut bir örnek.

**Gecikme:** steady-state parse ~900-2800ms - önceki oturumda vLLM kapalıyken
ölçülen ~15ms fallback yoluna göre **~100-200x daha pahalı**. Tek oturum,
ardışık (eşzamanlı yük yok) istekler - gerçek prod yükü altında nasıl
davranacağı hâlâ bilinmiyor.

## YOLO araç sayımı — AU-AIR gerçek etiketlerine göre BÜYÜK ÖLÇÜDE EKSİK SAYIYOR

Bizim `vehicle_count`'un (COCO ön-eğitimli YOLO26, aerial için fine-tune
EDİLMEDİ) AU-AIR'in kendi `bbox` etiketleriyle (max eş-zamanlı, sadece
`Car/Truck/Motorbike/Bus` - `Van/Bicycle/Trailer`'ın COCO karşılığı yok,
adil karşılaştırma için GT'den de dışlandı) karşılaştırılması:

| video | pencere | MAE | birebir doğru |
|---|---|---|---|
| auair_frame_20190829091111 | 12 | 2.25 | 2/12 |
| auair_frame_20190905091750 | 22 | 8.23 | 1/22 |
| auair_frame_20190905103112 | 25 | 23.32 | 0/25 |
| auair_frame_20190905111947 | 3 | 15.00 | 0/3 |
| auair_frame_20190905112522 | 21 | 16.86 | 0/21 |
| auair_frame_20190905142119 | 12 | 15.00 | 0/12 |
| auair_frame_20190905143505 | 11 | 14.73 | 0/11 |
| auair_frame_20190906150731 | 26 | 9.38 | 1/26 |

**TÜMÜ: N=132, MAE=13.45, birebir doğru 4/132 (%3), ortalama fark -13.38
(YOLO sistematik olarak EKSİK sayıyor).**

Bu rastgele bir hata değil - sistemik ve büyük. En olası sebep: **açı
domaini uyumsuzluğu**. COCO neredeyse tamamen yerden/yandan çekilmiş araç
görüntüsü içeriyor, AU-AIR ise düşük irtifadan (6.5-29.8m, bkz. yukarı) dik
aşağı (nadir) bakan bir İHA görüntüsü - bir aracın tepeden görünümü COCO'nun
eğitim dağılımında pratikte yok. `ingest/activities/visual_fields.py`'nin
docstring'i bunu şimdiye kadar sadece IR/termal için "fine-tune edilmedi"
diye işaretlemişti - bu ölçüm, aynı riskin **RGB aerial (nadir açı) için de**
gerçek ve büyük olduğunu somut olarak gösteriyor. İkincil bir katkı da
olabilir: `count_vehicles()` her 60sn'lik pencerede sadece birkaç örnek kare
alıyor (`config.YOLO_SAMPLE_FRAMES`), trafiğin en yoğun anını kaçırabilir -
ama -13.4'lük ortalama fark büyüklüğü tek başına örneklemeyle açıklanamaz.

**Sonuç: mevcut `vehicle_count` alanı gerçek İHA irtifasından nadir açıyla
çekilmiş görüntülerde (AU-AIR gibi) GÜVENİLİR DEĞİL.** Bu filtreye dayanan
herhangi bir üretim kararından önce aerial-fine-tune'lu bir YOLO
gerekecektir - proje-ozeti.md §3.1'in IR için zaten öngördüğü fine-tune
ihtiyacının RGB'de de geçerli olduğuna dair ilk somut kanıt.

### `min_vehicle_count≥10` hard filtresinin gerçek recall'ü: %16

Yukarıdaki eksik-sayım sorununun somut sonucu ölçüldü: 132 penceredeki
`(yolo_count, gt_count)` çiftleri üzerinden `vehicle_count≥10` eşiği için
precision/recall:

| | GT≥10 (108 pencere) | GT<10 (24 pencere) |
|---|---|---|
| YOLO≥10 | TP=17 | FP=0 |
| YOLO<10 | FN=91 | TN=24 |

**Precision=1.00, Recall=0.16.** YOLO "10+ araç var" dediğinde hiç
yanılmıyor (sıfır yanlış-pozitif) - ama gerçekte 10+ araç içeren 108
pencerenin **91'ini (%84) sessizce kaçırıyor**. Bir `WHERE vehicle_count >=
10` hard filtresi kullanılsaydı, doğru cevapların ezici çoğunluğu hiç
dönmeyecekti.

**Bu, CLAUDE.md'de zaten kayıtlı "Hard filtre + otomatik gevşetme" kararının
dayandığı bulgu türüyle (irtifa filtresinin 21 sorgunun 17'sinde doğru
cevabı yapısal olarak dışlaması, SeaDroneSee'de ölçülmüştü) AYNI kategoride
- ikinci, bağımsız bir veri setinde, hatta daha kötü bir oranla (%16 recall)
doğrulanmış hali.** Otomatik gevşetme mekanizmasının (§ proje-ozeti.md,
`query/hybrid_search.py` merdiveni) neden mimari olarak zorunlu olduğuna
dair ek bir kanıt - bu bulgu vLLM'in üretebileceği HERHANGİ bir
`min_vehicle_count` filtresi için geçerli, sadece "10" eşiğine özgü değil.

## Doğrulanmayan / eksik kalan

- `EMBEDDING_BATCH_SIZE` kalibre edilmedi (bkz. yukarı).
- Reconstruction hâlâ ~5 FPS ayrık kareden - gerçek çekim değil (bkz.
  `poc/auair_build_videos.py` docstring'i).
- vLLM eşzamanlı yük altında hiç test edilmedi (bu oturumda ardışık, tek
  istekli sorgular) - gerçek prod gecikmesi bilinmiyor.
- "Tamamen semantik sorgularda filtre çıkmamalı" varsayımı bu oturumda
  YANLIŞLANDI (yukarı bkz.) - kaç sorguda/ne sıklıkla oluyor ölçülmedi, N=1.

## Lisans hatırlatması

AU-AIR CC BY-NC-SA/CC BY-NC - bu tamamen iç doğrulama denemesi, kalıcı bir
ingest yolu değil. Sonuç: mekanizma (ingest/pencereleme/embedding/YOLO/Qdrant
yazımı) uçtan uca çalışıyor, ama hiçbiri kalite/dataset kararına doğrudan
girdi değil.
